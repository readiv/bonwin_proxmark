# bonwin_proxmark

Для восстановления алгоритма формирования ключей KeyA в зависимости от UID.

Прога генерирует 4*256 uid и по каждому из них пытается прочитать ключ из замка. 
Для чтения используется Proxmark3 с iceman прошивкой. 